# Changelog

## [Unreleased] - 2026-07-08

### Added
- Premium landing page design and styles (`index.html`, `main.css`).
- Premium login and dashboard layouts (`login.html`, `dashboard.html`, `teacher_dashboard.html`, `student_dashboard.html`).
- Glass-panel UI updates for admin pages and modals (`admin.html`, `admin.css`).

### Fixed
- JS interactions: counters trigger on load/scroll, theme toggle persistence, lightbox guards, testimonial carousel behavior (`landing.js`).

### Notes
- Run a full browser refresh (Ctrl+F5) to see updated assets and clear cache.
- Consider reviewing `login.html` and `admin.html` translations and localStorage data during testing.
